{
  "name": "@merkat/desktop",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build",
    "lint": "echo \"(desktop: Rust lint via cargo clippy)\" && exit 0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.1.0"
  }
}
